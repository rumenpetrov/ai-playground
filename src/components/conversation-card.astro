---
import {
  Card,
  CardHeader,
  CardTitle,
} from '@/components/ui/card';

interface Props {
  name: string;
}

const { name } = Astro.props;
---
<Card>
  <CardHeader>
    <CardTitle>{name}</CardTitle>
  </CardHeader>
</Card>
